import {initDropdownMobileButtonEvent} from "./event.js";

/**
 * Entry point for the mobile navigation component.
 * * @description Initializes the responsive menu behavior by setting up 
 * the necessary event listeners for the mobile dropdown.
 */
export function initDropdownMobile(){
    initDropdownMobileButtonEvent();
}